class Product {
    constructor(productType, options, artistMarkup, quantity) {
        this.productType = productType
        this.options = options
        this.artistMarkup = artistMarkup
        this.quantity = quantity
    }
}

module.exports = Product
